
Instructions to compile and run the ImagesToChar project:

1. Compile the source code:
   Navigate to the root of the project folder and execute:
   javac -d target/ src/**/*.java

2. Run the application:
   From the root of the project folder, execute the following command to run the application:
   java -cp target fr._42.printer.app.App
